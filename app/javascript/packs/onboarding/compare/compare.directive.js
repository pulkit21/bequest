function compareDirective() {
  return {
    controller: compareController,
    controllerAs: 'compareCtrl',
    link: compareLink,
    templateUrl: 'onboarding/compare/compare.html'
  };

  function compareController() {
  }

  function compareLink() {

  }
}

export default compareDirective;
